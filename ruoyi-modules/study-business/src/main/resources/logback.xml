<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="true" scanPeriod="60 seconds">
  <!-- 日志存放路径 -->
  <appender class="ch.qos.logback.core.ConsoleAppender" name="console">
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <!--格式化输出（配色）：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
      <charset>UTF-8</charset>
      <pattern>%yellow(%d{yyyy-MM-dd HH:mm:ss}) %red([%thread]) %highlight(%-5level)
        %cyan(%logger{50}) - %magenta(%msg) %n
      </pattern>
    </encoder>
  </appender>
  <!-- 日志输出格式 -->
  <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="file_info">
    <encoder>
      <pattern>${log.pattern}</pattern>
    </encoder>
    <!-- 循环政策：基于时间创建日志文件 -->
    <file>${log.path}/info.log</file>
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <!-- 过滤的级别 -->
      <level>INFO</level>
      <!-- 匹配时的操作：接收（记录） -->
      <onMatch>ACCEPT</onMatch>
      <!-- 不匹配时的操作：拒绝（不记录） -->
      <onMismatch>DENY</onMismatch>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <!-- 日志文件名格式 -->
      <fileNamePattern>${log.path}/info.%d{yyyy-MM-dd}.log</fileNamePattern>
      <!-- 日志最大的历史 60天 -->
      <maxHistory>60</maxHistory>
    </rollingPolicy>
  </appender>

  <!-- 控制台输出 -->
  <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="file_error">
    <encoder>
      <pattern>${log.pattern}</pattern>
    </encoder>
    <!-- 循环政策：基于时间创建日志文件 -->
    <file>${log.path}/error.log</file>
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <!-- 过滤的级别 -->
      <level>ERROR</level>
      <!-- 匹配时的操作：接收（记录） -->
      <onMatch>ACCEPT</onMatch>
      <!-- 不匹配时的操作：拒绝（不记录） -->
      <onMismatch>DENY</onMismatch>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <!-- 日志文件名格式 -->
      <fileNamePattern>${log.path}/error.%d{yyyy-MM-dd}.log</fileNamePattern>
      <!-- 日志最大的历史 60天 -->
      <maxHistory>60</maxHistory>
    </rollingPolicy>
  </appender>

  <!-- 系统日志输出 -->
  <logger level="info" name="com.ruoyi"/>

  <logger level="warn" name="org.springframework"/>

  <!-- 系统模块日志级别控制  -->
  <property name="log.path" value="logs/study--business"/>
  <!-- Spring日志级别控制  -->
  <property name="log.pattern"
    value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - [%method,%line] - %msg%n"/>

  <root level="info">
    <appender-ref ref="console"/>
  </root>

  <!--系统操作日志-->
  <root level="info">
    <appender-ref ref="file_info"/>
    <appender-ref ref="file_error"/>
  </root>

  <springProfile name="dev,test">
    <logger level="INFO" name="org.springframework.web"/>
    <logger level="INFO" name="org.springboot.sample"/>
    <logger level="DEBUG" name="com.ruoyi"/>
  </springProfile>

  <springProfile name="prod">
    <logger level="ERROR" name="org.springframework.web"/>
    <logger level="ERROR" name="org.springboot.sample"/>
    <logger level="ERROR" name="com.ruoyi"/>
  </springProfile>

</configuration>